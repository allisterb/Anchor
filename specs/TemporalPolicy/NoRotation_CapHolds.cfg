SPECIFICATION Spec

\* A run ends after MaxSteps, which has no successor action.
CHECK_DEADLOCK FALSE

CONSTANTS
    MaxAmount = 2
    MaxSteps = 6
    Gate = "aggregate"
    MayRotate = FALSE

INVARIANT TypeOK

\* THE CONTROL. Same policy, same adversary, rotation disabled -- and the cap holds.
\* Without this the violation next door would not be attributable to rotation.
INVARIANT GlobalCapHolds
