\* TL4 exactly as the paper publishes it. EXPECTED TO FAIL: the property forbids
\* cancelling a task that is being dispatched. Kept as a checked artifact so the
\* finding cannot rot into a stale comment.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE

CONSTANTS
    MaxRetries = 2
    MaxFallbacks = 1

PROPERTY TL4_AsPublished
